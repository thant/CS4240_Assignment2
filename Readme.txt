Left controller joystick to move reticle
Left controller side trigger to select (use to click button on start screen)
Left controller back trigger to bring model closer to camera (move reticle to model then press the trigger)

Right controller joystick to rotate spinal model
Right controller side trigger to switch spinal models
Right controller back trigger to reset model position

Info panel on left in visual stage continually updates based on active model.